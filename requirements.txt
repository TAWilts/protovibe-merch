Flask==3.1.0
gunicorn==23.0.0
cryptography>=45,<47
PyOTP>=2.9,<3
Pillow>=10,<12
qrcode[pil]>=8,<9
segno>=1.6,<2
# SQLCipher is required in production. It encrypts both SQLite databases at
# rest; app.py deliberately refuses to fall back to normal sqlite3.
sqlcipher3>=0.6.2,<0.7
