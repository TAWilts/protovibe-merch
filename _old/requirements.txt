Flask==3.1.0
gunicorn==23.0.0
cryptography>=45,<47
PyOTP>=2.9,<3
Pillow>=10,<12
qrcode[pil]>=8,<9
segno>=1.6,<2
# Transitional dependency only: the first plaintext release uses SQLCipher to
# read an existing encrypted installation exactly once and convert all durable
# data to ordinary SQLite/files. Remove this after the migration window.
sqlcipher3>=0.6.2,<0.7
