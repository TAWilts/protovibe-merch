# Production configuration for the Synology DS225+.
#
# Unlike docker-compose.yml, this file does not build source code on the NAS.
# It pulls the tested, immutable release image generated by GitHub Actions.
# MERCH_IMAGE_TAG chooses that image; its internal APP_VERSION already comes
# from the GitHub release tag used at image-build time.
# Keep the existing .env and data/ directory beside this file.
services:
  merch:
    image: ghcr.io/tawilts/protovibe-merch:${MERCH_IMAGE_TAG:-v0.3.0}
    container_name: protovibe-merch
    restart: unless-stopped
    ports:
      - "${HOST_PORT:-8088}:8000"
    environment:
      SECRET_KEY: ${SECRET_KEY}
      ADMIN_USERNAME: ${ADMIN_USERNAME}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      DATA_DIR: /data
      BACKUP_RETENTION_DAYS: ${BACKUP_RETENTION_DAYS:-90}
      ACCOUNT_SETUP_CODE_DAYS: ${ACCOUNT_SETUP_CODE_DAYS:-14}
      PROFILE_REAUTH_SECONDS: ${PROFILE_REAUTH_SECONDS:-600}
      MFA_ISSUER: ${MFA_ISSUER:-Protovibe Merch Manager}
      EMAIL_NOTIFICATIONS_ENABLED: ${EMAIL_NOTIFICATIONS_ENABLED:-false}
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-465}
      SMTP_SECURITY: ${SMTP_SECURITY:-ssl}
      SMTP_USERNAME: ${SMTP_USERNAME:-}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      SMTP_FROM: ${SMTP_FROM:-}
      ADMIN_NOTIFICATION_EMAIL: ${ADMIN_NOTIFICATION_EMAIL:-}
      SMTP_TIMEOUT_SECONDS: ${SMTP_TIMEOUT_SECONDS:-8}
      UPDATE_CHECK_REPOSITORY: ${UPDATE_CHECK_REPOSITORY:-TAWilts/protovibe-merch}
      UPDATE_CHECK_TOKEN: ${UPDATE_CHECK_TOKEN:-}
      UPDATE_CHECK_TIMEOUT_SECONDS: ${UPDATE_CHECK_TIMEOUT_SECONDS:-3}
      UPDATE_CHECK_CACHE_SECONDS: ${UPDATE_CHECK_CACHE_SECONDS:-21600}
      # Optional scheduled-update unlock. The values remain in root-only files
      # under this read-only mount; never put either secret into .env.
      SCHEDULED_RESTART_AUTH_TOKEN_FILE: /run/protovibe-scheduled-restart/authorisation-token
      SCHEDULED_RESTART_UNLOCK_SECRET_FILE: /run/protovibe-scheduled-restart/one-time-unlock-secret
      SCHEDULED_RESTART_UNLOCK_TTL_SECONDS: ${SCHEDULED_RESTART_UNLOCK_TTL_SECONDS:-1200}
    volumes:
      # Database and automatic backups persist through every image update.
      - ./data:/data
      # Disabled by default. Set SCHEDULED_RESTART_SECRETS_DIR to a root-only
      # DSM directory outside the project and data/ directories to opt in.
      - ${SCHEDULED_RESTART_SECRETS_DIR:-./scheduled-restart-secrets-disabled}:/run/protovibe-scheduled-restart:ro
